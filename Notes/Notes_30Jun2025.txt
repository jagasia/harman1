Microservices 
	Eureka service discovery
	Load balancing
	Circuit Breaker
	Feign client
	cloud/ api gateway

Spring cloud gateway
-------------------------
	we are going to provide on microservice that is going to be the single point for all clients. This gateway will redirect the request to other microservices based on the "path" they choose.

for example:
	there are 3 microservices running and their endpoints are

http://localhost:8081		for customer
http://localhost:8082		for product
http://localhost:8083		for sale

but instead of allowing users to discover these microservices by themselves, we are providing a gateway that runs in 8080

gateway runs in 8080

user input:
	http://localhost:8080/customer
	http://localhost:8080/product
	http://localhost:8080/sale


this 1 gateway, is receiving all requests and redirect to corresponding service.

Demo:
---------
refer to document
📘 Spring Cloud Gateway


----------------------
Jenkins
Sonarqube
Docker
AWS
	ECS
	EKS





Jenkins
----------
	we can run our java code using jenkins
	we can pull our java code from git and run

CI/CD can be implemented

we can download jenkins.war from 
https://get.jenkins.io/war-stable/2.504.3/jenkins.war




lets run jenkins.war

	java -jar jenkins.war





-----------------------
SonarQube
-------------
	a tool used for testing the code quality
	it is not testers tool.
	it is developer's tool to constantly learn and implement
		coding standars and good practices

there are lot of metrics like
	code smell
	coverage
	
we can download sonarqube and start the server.



steps:

1) start sonarqube 

D:\Softwares\sonarqube-8.8.0.42792\bin\windows-x86-64

StartSonar.bat

2) localhost:9000


3) create a maven project with poor coding


import java.util.Date;

public class Example {

	public String speak() {
		int i;
		Date dt=null;
		System.out.println(dt.toString());
		int k=0;
		if(k==0)
			return "Ok";
			
		return "Hello";
		
	}
}


sonarqube steps:

1) in sonar admin portal,

click "Add project" button
	Manually

project key	= project name


setup




mvn sonar:sonar \
  -Dsonar.projectKey=harman-r \
  -Dsonar.host.url=http://localhost:9000 \
  -Dsonar.login=39a2b44e771bd3e27ed56b3af419e451b5a11827



sonar:sonar -Dsonar.projectKey=harman-r -Dsonar.host.url=http://localhost:9000  -Dsonar.login=39a2b44e771bd3e27ed56b3af419e451b5a11827


in eclipse
	right click project
		run as 
			maven build


paste the goals


run

now sonar portal automatically refreshes and show you the report.


Task:
-------
Once i push code to git

you can check 
	Spring cloud gateway
	Jenkins
	Sonarqube

------------------
Docker
--------

virtualization

even before start of our training,
we are all provided with cloud lab

that is a virtual computer, there, everything is installed
and they have cloned it.

what is the difference between 
	virtual PC or vmware and Docker

virtul pc is heavy weight
	the entire OS, desktop, is virtualized
	
whereas in case of a docker image
	only the required components are packed
	
so docker is light weight.

docker steps:

1) install WSL 	
	windows sub system for Linux
2) install docker for windows

	docker -v

Docker version 28.0.1, build 068a01e

docker demon (service) should be running

then only we can see docker images

docker images
or
docker image ls


what is a docker image?
	in those days, when there were some CDs that works only when you put cd in cd drive

we used to create ISO image of cd
so that we can mount it and play

like that, the containerized copy of our deployable project unit is called as docker image

when we run that docker image,
	it becomes docker container

to see all containers
	docker container ls
	or
	docker ps
	or 
	docker ps -a		for all including background

docker rmi <<image name>>	to remove an image
docker rm <<container id>		to remove container
-f			to force remove even when it is running

docker build		to build image
docker run		to run image
docker pull		to pull image from cloud (hub.docker.com)
docker push		to pull local image to hub.docker.com

docker compose up		to run multiple images/services together using docker_compose.yml file

docker network
docker -it


lets see how we can run a docker image
---------------
for example

if you do not have mysql in your computer.
we can pull and run mysql image from docker hub.


### ✅ **Step 1: Pull MySQL 8 Docker Image**

```bash
docker pull mysql:8
```

---

### ✅ **Step 2: Run MySQL 8 Container**

```bash
docker run -d \
  --name mysql8-container \
  -e MYSQL_ROOT_PASSWORD=root123 \
  -p 3306:3306 \
  mysql:8
```

**Explanation:**

* `-d`: Run in detached mode.
* `--name mysql8-container`: Container name.
* `-e MYSQL_ROOT_PASSWORD=root123`: Set root password.
* `-p 3306:3306`: Expose port 3306 to host.

---

### ✅ **Step 3: Access Container Bash**

```bash
docker exec -it mysql8-container bash
```

Once inside the container, you'll see a bash prompt like:

```bash
root@<container_id>:/#
```

---

### ✅ **Step 4: Login to MySQL from Inside Bash**

```bash
mysql -u root -p
```

Then enter the password you set earlier (`root123` in this example).

---

### ✅ **Optional: View Running Containers**

```bash
docker ps
```

---

### ✅ **Stop the Container (When Done)**

```bash
docker stop mysql8-container
```

---

### ✅ **Start Again (If Already Created)**

```bash
docker start mysql8-container
```











Lets run an existing application in docker.
1) open cmd / terminal
2) 
3) docker run -p 5000:5000 in28min/todo-rest-api-h2:1.0.0.RELEASE
4) http://localhost:5000/hello-world
	check this in a browser
	(also)
http://localhost:5000/hello-world-bean

5) 
	i) To check the running containers list
		docker container ls
	ii) To check all the containers list
		docker container ls -a
6) to kill a container
	docker stop <<container-id>>
	docker container stop <<container-id>>

	stop vs kill	(stop gracefully. Kill immediately)

7) to run a docker container in the background
	docker run -p 5000:5000 -d in28min/todo-rest-api-h2:1.0.0.RELEASE
8) to check the logs,
	docker logs <<container-id>>
	(also)
	docker logs -f <<first 5 digits of container id>>
9) docker images
	to list the images
-------------------------------------------
Task:
------
Identify the docker commands
and how to run an existing docker image
and list the containers
and kill the container

After break, we will learn to create a docker image of a spring boot application.

Demo:

lets create a docker image of our spring boot application.

there are 2 ways we can do this.
	1) using docker file
	2) maven spring boot project
		mvn spring-boot:build-image
	
		this will not require docker file.


we have taken 
	26-mvc-crud 
in microservices workspace.

it is now h2 database (file system)


in cmd prompt, where pom.xml is present
	mvn spring-boot:build-image


to run this image

docker run -p 5001:5000 26-mvc-crud:0.0.1-SNAPSHOT


go to browser:

http://localhost:5001

you can see home page.



now this image can be pushed to hub.docker.com

and then you can pull it and run in any computer, where just docker is installed.

i will also show you, how to 	
	install docker in aws
	and pull and run this project in aws
	and so that, anybody can access our app
		from browser in mobile or computer


Demo:

how to push this docker image to hub.docker.com?

1) go to hub.docker.com
	login to your account

2) create a respository
	"26-mvc-crud"

3) in cmd prompt (any location)
	tag docker image to repository

docker tag 26-mvc-crud:0.0.1-SNAPSHOT jagindia/26-mvc-crud

4) push the image to hub

	docker push jagindia/26-mvc-crud




------------------------------------------
suppose, one docker image of a project that connects with mysql,
and we have a docker image of mysql.

totally 2 docker images.

both should run together and our project should access mysql also.

docker compose should be used.



create a Dockerfile in project folder where pom.xml is present

# Dockerfile
FROM eclipse-temurin:21-jdk-alpine
WORKDIR /app
COPY target/*.jar app.jar
EXPOSE 5000
ENTRYPOINT ["java", "-jar", "app.jar"]


create docker compose yml
docker-compose.yml

version: '3.8'

services:
  mysql:
    image: mysql:8
    container_name: mysql8-container
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: harman2
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

  springboot-app:
    image: jag-mysql-crud
    container_name: springboot-app-container
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/harman2
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: root
    depends_on:
      - mysql

volumes:
  mysql_data:





mvn clean package -DskipTests


docker compose up --build


Task:

in cloud lab,
you can try creating docker image
and run them as container

check
	docker ps -a

kill the containers after use.



-----------------------
-----------------------
AWS
	Amazon Web Services

is cloud

basically, imo, a cloud can provides
	storage services		S3
	compute services		EC2
	network services		VPC
and so on

Software as a service
Platform as a service
Infrastructure as a service


Cloud computing is agile in provisioning and de-provisioning with minimal or no human intervention.

Introduction to AWS Cloud and Microservices					

using aws, how we can deploy microservices?

anybody can signup and use aws.


ECS
	Elastic Container Services
		allows us to run containers in aws


since we have already pushed a docker image to hub.docker
we can run that docker image in ECS

docker.io/jagindia/26-mvc-crud:latest
	this is the url for our docker image in hub.




ECS allows me to create a cluster and run the container.

what is a cluster?
	a cluster is a combination of nodes

node has container, in which we deploy the pods

Demo:
--------
we will first create a task in ECS.
then create a cluster and run the task in that cluster

once our docker image run as container in aws,
you all can access my project using the url.

1) login to aws.amazon.com
and search for ECS

2) create a task definition

-----------------------------------
EC2
	Elastic Compute Cloud

1) how to launch a windows instance
2) how to launch a linux instance
	how to install docker
	and run a docker hub image in ec2 instance
	and allow everyone to access our app/ service




we have launched windows instance and connected using Remote Desktop client
and terminated instance after use.


now, we have launched a linux instance and connected using ssh client.

it will connect when the .pem file is present in that location


You can try upto this.

After break, i will show you
	how to install docker in linux instance
	and then pull our docker hub image
	run it in aws

and enable access to all

so that, the docker image running as container in aws linux, we can access the website/service from our mobile or computer.


------------------------------------
Deploying reactjs app in aws.
----------------------------------
after completing and testing the project,
convert the app into static website.
	npm run build

this will create a "build" folder that contains 
	html, css, js files



Today we discussed:

Spring cloud gateway
Jenkins
Sonar Qube
Docker
AWS ECS
AWS EKS
AWS EC2
AWS deploying reactJs in s3 bucket and state hosting





Kafka
---------
streaming
async messaging
public /subscribe model
	producer / consumer

practically, we can see a demo, where
	one producer publishes the message
	and whoever have subscribed (consumer) will get that message

Topic	
Queue


docker images of this kafka, we can run




Kafka services and MSK (Managed Services for Kafka)
-----------------------
	what is kafka service?

		Publish - Subscribe model

	Publisher = Producer
	Subscriber = Consumer

See practical demo:
-------------------
	The producer and consumer are not online.
	They are related via "Topic"
		JMS	i) queue	ii) topic

		

 docker
	use docker images
		kafka
		zookeeper
		
	how to run two docker images together?
		docker-compose.yml

0) create a "docker-compose.yml" file:
-----------------------------------
version: '2'
services:
  zookeeper:
    image: wurstmeister/zookeeper
    ports:
      - "2181:2181"
  kafka:
    image: wurstmeister/kafka
    ports:
      - "9092:9092"
    environment:
      KAFKA_ADVERTISED_HOST_NAME: 127.0.0.1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: 'false'

1) docker compose up
	in a location in cmd prompt where "docker-compose.yml" is present

	this is going to run the docker images 

2) lets create a producer
	open a new cmd prompt
C:\Users\rjaga>docker ps
CONTAINER ID   IMAGE                    COMMAND                  CREATED             STATUS             PORTS                                                NAMES
98a2ea29060f   wurstmeister/kafka       "start-kafka.sh"         About an hour ago   Up About an hour   0.0.0.0:9092->9092/tcp                               kafka_kafka_1
2c9ea685d4f3   wurstmeister/zookeeper   "/bin/sh -c '/usr/sb…"   About an hour ago   Up About an hour   22/tcp, 2888/tcp, 3888/tcp, 0.0.0.0:2181->2181/tcp   kafka_zookeeper_1

C:\Users\rjaga>docker exec -it 98a2ea29060f bash
bash-5.1# kafka-topics.sh --create --zookeeper zookeeper:2181 --replication-factor 1 --partitions 1 --topic test
Created topic test.
bash-5.1#


bash-5.1# kafka-console-producer.sh --broker-list localhost:9092 --topic test
>this is the message
abcd
>efgh
>ijkl
>emop
>qrst



3) lets create a receiver
docker ps -a
	note down the container id of kafka
C:\Users\rjaga>docker exec -it 98a2ea29060f bash
bash-5.1# kafka-console-consumer.sh --bootstrap-server localhost:9092 --topic test
this is also a message
abcd
1234
efgh
ijkl
mnop
qrst

------------------------------------------
demo for image in entity

1) create a spring boot app
	employee-service
	
	spring web
	lombok
	spring data jpa
	mysql driver

2) application.properties

spring.application.name=employee-service

server.port=5000

spring.datasource.url=jdbc:mysql://localhost:3306/ust2
spring.datasource.username=root
spring.datasource.password=

spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.maximum-pool-size=12
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1200000

spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.id.new_generator_mappings = false
spring.jpa.properties.hibernate.format_sql = false
spring.jpa.hibernate.ddl-auto=update
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE




3) entity:
package com.ust.demo.entity;

import java.util.Date;

import jakarta.persistence.Entity;
import jakarta.persistence.GeneratedValue;
import jakarta.persistence.GenerationType;
import jakarta.persistence.Id;
import jakarta.persistence.Table;
import lombok.Data;

@Entity
@Table(name = "EMP")
@Data
public class Employee {
	@Id
	@GeneratedValue(strategy = GenerationType.AUTO)
	private Long id;
	private String name;
	private Date dob;
	@Lob
	private String photo;
	
}


4) repo

5) service

6) controller




firstName		=	FIRST_NAME


