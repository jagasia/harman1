apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: mvc-cluster
  region: ap-south-1
  version: "1.28"

vpc:
  nat:
    gateway: Disable

nodeGroups:
  - name: ng-public
    instanceType: t3.small
    desiredCapacity: 2
    minSize: 1
    maxSize: 3
